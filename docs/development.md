# 开发与运行

## 操作顺序

1. 基于 `.env.example` 创建本地环境变量文件，填入仅限本地/测试环境的值。
2. 先启动基础设施编排，再启动所需应用编排。
3. 检查容器状态、服务日志、配置中心注册和关键 HTTP 健康检查。

## 变更要求

- 新增服务时同步维护相应 Compose 文件、配置样例和启动验证步骤。
- 数据库脚本必须在代码依赖新表/新数据之前执行。
- 生产部署使用独立流程；本仓库的本地 Compose 说明不能替代生产变更审批。
